digraph{
    graph [
        bgcolor="#FFFFFFF00",
        rankdir=LR,
        ];

    released -> fetch;
    fetch -> flight;
    flight -> fetch;
    fetch -> missing;
    missing -> fetch;
    flight -> memory;
}
